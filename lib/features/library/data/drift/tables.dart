/// Drift table definitions (placeholder — real codegen on mobile builds).
/// Web demo uses [WebDemoRepository] instead of Drift.
library;

// Tables would normally be defined here for Drift code generation.
// Kept minimal so the web demo compiles without generated *.g.dart files.
